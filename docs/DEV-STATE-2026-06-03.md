# Development State Snapshot — 2026-06-03

Point-in-time record of project state after completing the full MAC-RAG
implementation plan (W1–W12) and the Phase-4b memory loop. Both worktrees
are at `origin/main` (`ad5f2a0`).

---

## 1. Headline status

- **Code**: All Phase-4b memory-write paths, QA surface, Phase-3 triage
  UI, two-stage MAC-RAG pipeline, L2/L3/L4 hierarchical context, and
  paragraph-boundaries fix are all shipped and on `origin/main`.
- **Docs**: This file + all W1–W12 work units marked DONE in
  `MAC-RAG-EXAMPLES-TODO-PLAN.md`; `PARA-BOUNDARIES-IMPL` closed in
  `BACKEND-FOLLOWUP-FE-COORD.md`.
- **Git**: Both worktrees clean at `origin/main` (`ad5f2a0`). No unpushed
  commits.
- **tsc**: `npx tsc --noEmit` exits 0 in both worktrees.
- **Playwright**: baseline 85 passed / 2 failed / 1 skipped (the 2
  failures are FE-territory book-size bugs documented in
  `FE-COORD-BOOKSIZE-SCALING-2026-05-30.md`).

---

## 2. What is in `origin/main` (as of `ad5f2a0`)

### Phase-4b memory loop (backend lane)

| Commit | What |
|---|---|
| `293baa3` | Phase-4b accept→rpc_phase_4b_translate_save write-back; retrieval layer repoint (tm_search_view, terminology_active_view) |
| `01aeb78` | Extended write-back to edit phase (rpc_phase_4b_edit_save, edit_pattern:null) |
| `b139194` | QA-issues API surface (GET/POST/PATCH /qa-issues); advisory QA agent (/agents/qa); qaPrompt(); 4 Playwright specs |
| `1082d08` | Phase-3 triage UI: EditPatternModal, StyleRuleModal, wired into SuggestionPanel + PATCH route + useSuggestions |
| `09c6a0d` | scope_ref wiring for article-level style_guide anchoring |
| `4df926e` | Phase-4 QA resolution screen: QAResolveModal, QAIssuesList, useQAIssues hook, rpc_phase_4b_qa_save wired |
| `fc35a12` | Post-Production Panel: MemoryWriteBanner surfaces Phase-4b memory write-back outcome |
| `75004fc` | Two-stage MAC-RAG: POST /api/mac-rag/compose (Stage 1, no LLM) + POST /api/mac-rag/generate (Stage 2); useMacRagTwoStage hook |
| `74cb7b5` | L2 article-local hierarchical context in compose: lib/context/article-context.ts (neighbours, title, terms_already_annotated) |
| `76543d9` | L3/L4 TM split in compose prompt; paragraph_boundaries fix in segmentize |

### FE lane (merged into main via FE PRs)

| Commit | What |
|---|---|
| `600a1dc` | Reader: font color picker, px font size |
| `44db1d7` | Reader: per-article page bookmarks (localStorage) |
| `aa7961c` | Profile: proper user profile page with stats, assignments, history |
| `b9439cc` | Nav: global SiteNav + profile link in documents header |
| `ad5f2a0` | Reader: sidebar (TOC + search), progress bar, scroll-to-top |

---

## 3. All Phase-4b RPCs: implementation status

| RPC | Triggered from | Status |
|---|---|---|
| `rpc_phase_4b_translate_save` | Accept on `draft` segment suggestion | ✅ wired (PATCH /suggestions/[id]) |
| `rpc_phase_4b_edit_save` | Accept on `translated` segment suggestion | ✅ wired (edit_pattern: null unless EditPatternModal fills it) |
| `rpc_phase_4b_save_style` | Accept on `edited` segment suggestion | ✅ wired (StyleRuleModal; skipped when fields absent) |
| `rpc_phase_4b_qa_save` | Resolve qa_issue with qa_save payload | ✅ wired (PATCH /qa-issues/[id]; QAResolveModal) |
| `rpc_phase_4b_promote_term` | Accept on any suggestion (promote_terms array) | ✅ wired (all three accept paths) |

---

## 4. MAC-RAG two-stage pipeline: implementation status

| Endpoint | Status | Notes |
|---|---|---|
| `POST /api/mac-rag/compose` | ✅ done | L1 (segment), L2 (article: neighbours/title/terms_already_annotated), L3/L4 TM split, terminology context |
| `POST /api/mac-rag/generate` | ✅ done | LLM call; prompt-edit audit; QA advisory flag |
| `useMacRagTwoStage` hook | ✅ done | compose + generate client hook |
| Context Builder Panel (UI) | ⚠️ API exists; UI shell not wired yet | The compose/generate endpoints are ready; no page-level React UI yet that renders the editable prompt between Stage 1 and Stage 2. |
| MAC-RAG → per-phase agents | ✅ done | compose route routes to translatePrompt/editPrompt/proofreadPrompt/qaPrompt |

---

## 5. Open threads

### Deferred (not in scope for current sprint)

- **Context Builder Panel page-level UI** — the `useMacRagTwoStage` hook and endpoints are ready; a dedicated React UI that shows the composed prompt as editable text between Stage 1 and Stage 2 is not yet built. This is the last FE gap from TODO 2.
- **`edit_pattern` Phase-3 full wiring** — `EditPatternModal` collects before/after phrase but `rpc_phase_4b_edit_save`'s `edit_pattern` is populated only when the modal is saved (not the skip path). This is the intended design; full edit_pattern capture deferred to Phase-3 UI.
- **`prompt_edits` table seeding** — `/api/mac-rag/generate` writes a `prompt_edits` audit row only when an `agent_prompts` row exists for the phase. No `agent_prompts` rows are seeded yet for phases other than `translate`.
- **L4 Cross-Lingual KB (Wikidata)** — out of scope for this sprint.
- **T3-B Importer parse artifacts** — Class A/B artifacts in trilingual importer; not authorized.
- **StyleRuleModal `scope_ref` for document-level scoping** — wired for article; document-level requires document_id lookup.
- **Playwright specs for Phase-3 modals** — deferred.

### FE-territory (tracked in coord docs)

- Bug A: segment-activity 500 on book-sized docs.
- Bug B: edit-page screenshot >32767px overflow.
  Both documented in `FE-COORD-BOOKSIZE-SCALING-2026-05-30.md`.

---

## 6. Key file map (as of this snapshot)

### New files (this sprint)

```
app/api/agents/qa/route.ts
app/api/mac-rag/compose/route.ts
app/api/mac-rag/generate/route.ts
app/api/segments/[id]/qa-issues/route.ts
app/api/segments/[id]/qa-issues/[issueId]/route.ts
components/editor/EditPatternModal.tsx
components/editor/MemoryWriteBanner.tsx
components/editor/QAIssuesList.tsx
components/editor/QAResolveModal.tsx
components/editor/StyleRuleModal.tsx
lib/agents/phase-prompts.ts           (qaPrompt added)
lib/context/article-context.ts
lib/hooks/useMacRagTwoStage.ts
lib/hooks/useQAIssues.ts
tests/qa-issues-api.spec.ts
```

### Modified files (key changes)

```
app/api/segments/[id]/suggestions/[suggestionId]/route.ts  — Phase-4b write-back (translate+edit+proofread paths)
app/api/segments/[id]/qa-issues/[issueId]/route.ts         — PATCH extended with qa_save
app/api/mac-rag/compose/route.ts                           — L2 context; L3/L4 TM split
app/api/documents/[id]/segmentize/route.ts                 — paragraph_boundaries fix
components/editor/SuggestionPanel.tsx                      — EditPatternModal/StyleRuleModal integration; MemoryWriteBanner
lib/hooks/useSuggestions.ts                                — AcceptMetadata; MemoryWriteResult
lib/retrieval/tm-search.ts                                 — repointed to tm_search_view; retrievalLayer
lib/retrieval/terminology.ts                               — repointed to terminology_active_view
```
